# Osi Vs Tcp Ip

Purpose: Where problems live.

OSI and TCP/IP are models to locate problems, not memorize layers.

Each layer has a single responsibility.

A failure at one layer affects all layers above it.
